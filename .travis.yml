compiler:
    - gcc

before_install:
    - sudo apt-get update
    - sudo apt-get install -y libopencv-dev python-opencv python-numpy python-flask

notifications:
    email: none

script:
    - python server.py test

